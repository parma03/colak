<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Eticket App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body style="padding-top: 56px;">
    <%- include('../partials/navbar') %>

    <div class="main-content">
        <div class="row">
            <div class="col-md-12">
                <div class="card shadow-sm">
                    <div class="card-body p-5">
                        <div class="text-center mb-4">
                            <i class="bi bi-house-door-fill text-primary" style="font-size: 4rem;"></i>
                            <h1 class="mt-3">Selamat Datang, <%= user.username %>! 👋</h1>
                            <p class="text-muted lead">Anda login sebagai: <span class="badge bg-primary"><%= user.role %></span></p>
                        </div>

                        <div class="row mt-5">
                            <div class="col-md-6">
                                <div class="card border-primary mb-3">
                                    <div class="card-header bg-primary text-white">
                                        <i class="bi bi-info-circle"></i> Informasi Akun
                                    </div>
                                    <div class="card-body">
                                        <p><strong>Username:</strong> <%= user.username %></p>
                                        <p><strong>Email:</strong> <%= user.email %></p>
                                        <p><strong>Role:</strong> <span class="badge bg-success"><%= user.role %></span></p>
                                    </div>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="card border-info mb-3">
                                    <div class="card-header bg-info text-white">
                                        <i class="bi bi-key"></i> Akses Menu
                                    </div>
                                    <div class="card-body">
                                        <ul class="list-unstyled">
                                            <li class="mb-2">
                                                <i class="bi bi-check-circle-fill text-success"></i> Dashboard
                                            </li>
                                            <% if (user.role === 'admin') { %>
                                            <li class="mb-2">
                                                <i class="bi bi-check-circle-fill text-success"></i> User Management
                                            </li>
                                            <% } else { %>
                                            <li class="mb-2">
                                                <i class="bi bi-x-circle-fill text-danger"></i> User Management (Admin Only)
                                            </li>
                                            <% } %>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <% if (user.role === 'admin') { %>
                        <div class="alert alert-info mt-4">
                            <i class="bi bi-info-circle"></i> 
                            <strong>Halo Admin!</strong> Anda memiliki akses penuh ke semua fitur termasuk User Management.
                            Klik menu <strong>User Management</strong> di sidebar untuk mengelola user.
                        </div>
                        <% } else { %>
                        <div class="alert alert-warning mt-4">
                            <i class="bi bi-info-circle"></i> 
                            <strong>Halo User!</strong> Anda saat ini hanya memiliki akses ke Dashboard.
                            Hubungi admin untuk upgrade akses.
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
